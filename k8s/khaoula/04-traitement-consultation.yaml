apiVersion: apps/v1
kind: Deployment
metadata:
  name: traitement-service
  namespace: mindcare
spec:
  replicas: 2
  selector:
    matchLabels:
      app: traitement-service
  template:
    metadata:
      labels:
        app: traitement-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8081"
    spec:
      containers:
        - name: traitement-service
          image: 121999121999/mindcare-traitement:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: mindcare-khaoula-config
            - secretRef:
                name: mindcare-khaoula-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: traitement-service
  namespace: mindcare
spec:
  selector:
    app: traitement-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
