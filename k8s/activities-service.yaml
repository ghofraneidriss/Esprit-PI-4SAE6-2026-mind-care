apiVersion: apps/v1
kind: Deployment
metadata:
  name: activities-service
  namespace: mindcare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: activities-service
  template:
    metadata:
      labels:
        app: activities-service
    spec:
      containers:
        - name: activities-service
          image: mindcare/activities-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka:8761/eureka"
            - name: SPRING_MAIN_ALLOW_BEAN_DEFINITION_OVERRIDING
              value: "true"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: activities-service
  namespace: mindcare
spec:
  selector:
    app: activities-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
