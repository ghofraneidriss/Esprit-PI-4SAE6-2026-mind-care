apiVersion: apps/v1
kind: Deployment
metadata:
  name: souvenir-service
  namespace: mindcare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: souvenir-service
  template:
    metadata:
      labels:
        app: souvenir-service
    spec:
      containers:
        - name: souvenir-service
          image: mindcare/souvenir-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka:8761/eureka"
            - name: SPRING_MAIN_ALLOW_BEAN_DEFINITION_OVERRIDING
              value: "true"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: souvenir-service
  namespace: mindcare
spec:
  selector:
    app: souvenir-service
  ports:
    - protocol: TCP
      port: 8086
      targetPort: 8086
  type: ClusterIP
