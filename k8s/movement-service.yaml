apiVersion: apps/v1
kind: Deployment
metadata:
  name: movement-service
  namespace: mindcare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: movement-service
  template:
    metadata:
      labels:
        app: movement-service
    spec:
      containers:
        - name: movement-service
          image: mindcare/movement-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8086
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka:8761/eureka"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:3306/movement_db?createDatabaseIfNotExist=true"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              value: ""
---
apiVersion: v1
kind: Service
metadata:
  name: movement-service
  namespace: mindcare
spec:
  selector:
    app: movement-service
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP
