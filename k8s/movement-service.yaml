apiVersion: apps/v1
kind: Deployment
metadata:
  name: movement-service
  namespace: mindcare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: movement-service
  template:
    metadata:
      labels:
        app: movement-service
    spec:
      containers:
        - name: movement-service
          image: mindcare/movement-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka:8761/eureka"
            - name: SPRING_MAIN_ALLOW_BEAN_DEFINITION_OVERRIDING
              value: "true"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: movement-service
  namespace: mindcare
spec:
  selector:
    app: movement-service
  ports:
    - protocol: TCP
      port: 8086
      targetPort: 8086
  type: ClusterIP
